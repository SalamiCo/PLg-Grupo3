@Programa para ver que el paso de parámetros desordenados lo hace bien

program: test11{
   
    vars {
    var integer x;
    var float y;
    var natural W;
    var integer z;
  }

	subprograms {
    subprogram: suma_variables(integer var1, float var2, natural var3, integer var4){ 
        instructions {
           vars {
              var float var;
            } 

            var = var1 + var2 + var3 + var4;
            out(var);
        }
    }

  }
    
	instructions {
     x = -5;
     y = 2.3;
     w = 1;
     z = 2;
    

     call suma_variables(var4 = z, var2 = y, var3 = w, var1 = x);
	}
}